import { products } from '@/lib/products'
export default function Checkout({searchParams}:{searchParams:{product?:string}}){const p=products.find(x=>x.slug===searchParams.product)||products[0];return <main className="section"><h2>Checkout</h2><div className="grid"><div className="card"><h3>{p.name}</h3><p className="muted">${p.price}</p><form action="/api/checkout" method="POST"><input type="hidden" name="slug" value={p.slug}/><label><input type="checkbox" required/> I agree to consent, compliance, and provider review requirements.</label><br/><br/><button className="btn" type="submit">Continue to Secure Payment</button></form></div><div className="notice">Stripe checkout requires environment variables in Vercel. Until connected, checkout will return setup instructions instead of charging.</div></div></main>}
