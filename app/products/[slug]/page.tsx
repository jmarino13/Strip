import { findProduct } from '@/lib/products'
import Image from 'next/image'
import Link from 'next/link'
export default function ProductPage({params}:{params:{slug:string}}){const p=findProduct(params.slug); if(!p)return <main className="section"><h2>Product not found</h2></main>; return <main className="section"><div className="hero"><div><span className="badge">{p.category}</span><h1>{p.name}</h1><p>{p.description}</p><ul>{p.benefits.map(b=><li key={b}>{b}</li>)}</ul><div className="price">${p.price}</div><div className="actions"><Link className="btn" href={`/checkout?product=${p.slug}`}>Buy Now</Link><Link className="btn secondary" href="/products">Back to Catalog</Link></div><p className="notice">This site is a commerce and wellness platform template. Final medical, pharmacy, prescribing, shipping, and advertising compliance should be reviewed by counsel and licensed medical leadership before launch.</p></div><div className="hero-card"><Image src={p.image} alt={p.name} width={800} height={700}/></div></div></main>}
